# Build Commands

This is an Android project using Gradle with Kotlin DSL.

## Build
- Debug APK: `./gradlew assembleDebug`
- Release APKs: `./gradlew assembleRelease`
- Lint: `./gradlew lint`

## Requirements
- JDK 17
- Android SDK 34 (compileSdk)
- Min Android 7.0 (API 24)
